# Broken demo app

This deliberately flawed, local fixture contains a fake placeholder token so the ReleaseProof flow has evidence to find. It has no network service and no production credential.

Run `npm start` to execute the tiny local example. No tests or release workflow are configured in this starting state.
