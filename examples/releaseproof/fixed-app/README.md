# Fixed demo app

This is the intentionally repaired ReleaseProof fixture. It keeps credentials in environment configuration, defines test and build commands, and runs the same checks in CI.

Run `npm test` and `npm run build` from this directory. These instructions are part of the fixture; the local ReleaseProof static profile checks configuration but does not execute project scripts.
